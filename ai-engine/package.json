{
    "name": "ai-engine",
    "version": "0.0.1",
    "description": "ai engine to make moves",
    "scripts": {
        "dev": "cross-env NODE_ENV=development nodemon random-ai.js",
        "start": "cross-env NODE_ENV=production node main.js",
        "test": "jest --runInBand"
    },
    "dependencies": {
        "cross-env": "^7.0.3",
        "nodemon": "^3.0.1",
        "cross-env": "^7.0.3",
        "socket.io-client": "^4.7.2"
    },
    "devDependencies": {
        "cypress": "^13.4.0",
        "jest": "^29.7.0",
        "jtest": "^0.0.1"
    }
}
